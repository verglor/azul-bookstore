<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.17.xsd">

    <changeSet id="005-create-book-genres-table" author="system">
        <comment>Create book_genres junction table for many-to-many relationship</comment>
        <createTable tableName="book_genres">
            <column name="book_id" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="genre_id" type="bigint">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Add primary key constraint -->
        <addPrimaryKey columnNames="book_id, genre_id" tableName="book_genres"/>

        <!-- Add foreign key constraints -->
        <addForeignKeyConstraint
            baseTableName="book_genres"
            baseColumnNames="book_id"
            referencedTableName="books"
            referencedColumnNames="id"
            constraintName="fk_book_genres_book_id"/>

        <addForeignKeyConstraint
            baseTableName="book_genres"
            baseColumnNames="genre_id"
            referencedTableName="genres"
            referencedColumnNames="id"
            constraintName="fk_book_genres_genre_id"/>
    </changeSet>

</databaseChangeLog>